{
  "name": "davydov-qr",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Projector-only QR page for qr.davydov-pr.com — retarget via TARGET_URL env, no rebuild scramble.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "uqr": "^0.1.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.131.0"
  },
  "engines": {
    "node": ">=20"
  }
}
